#data type
type=csv

# total image
size=10000 

# this field indicate the img name in @data_path, 
# only available when data type is img
data_path=/data/lz/sgx_project_v2/data/train.csv
result_path=./res

# image height
height=28
# image weight
width=28
#image channel
channel=1

# For once encrypt and transfer
batch = 512

file_list_path=

# this field indicate the label of every img in @data_path,
# corresponding to @file_list_path only available
label_path= 

